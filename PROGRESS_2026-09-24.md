# White Board 00 — Progress Handoff

Date: 24 September 2026 (Singapore)

## Completed today

### Solution and Dataverse

- Created and imported the unmanaged solution `WhiteBoard00Solution` into **Estimation Dev**:
  `https://org0e46434e.crm7.dynamics.com/`
- Confirmed the solution publisher is `MikeSolutions` with the `mtl` customization prefix.
- Verified the safe direct Dataverse route with the read-only `mtl_scheduledemail` canary.
- Created the solution-aware, user-owned Dataverse table:
  - Display name: `White Board Post`
  - Logical name: `mtl_whiteboardpost`
- Added and verified these columns:

  | Display name | Logical name | Type |
  | --- | --- | --- |
  | Title | `mtl_title` | Text, required, primary name |
  | Message | `mtl_message` | Multiline text, required |
  | Category | `mtl_category` | Choice |
  | Priority | `mtl_priority` | Choice |
  | Is Pinned | `mtl_ispinned` | Yes/No |
  | Publish From | `mtl_publishfrom` | Date and time, user local |
  | Expires On | `mtl_expireson` | Date and time, user local |
  | Status | `mtl_status` | Choice: Draft, Published, Archived |
  | Attachment | `mtl_attachment` | File, maximum 10 MB |

- Published customizations in Estimation Dev.
- Exported and unpacked the current remote solution source into `WhiteBoard00Solution-exported/`.
- Successfully packed the exported source as `WhiteBoard00Solution-verified-unmanaged.zip`.

### Canvas App

- Created a blank **Tablet** Canvas App named `White Board 00` inside `WhiteBoard00Solution`.
- Renamed the original screen to `scrBoard`.
- Added the Dataverse data source **White Board Posts**.
- Added and renamed a vertical gallery: `galPosts`.
- Set the gallery layout to **Title, subtitle, and body**.
- Set `galPosts.Items` to show published, currently valid posts with pinned posts first:

```powerfx
SortByColumns(
    Filter(
        'White Board Posts',
        mtl_status = 'Status (White Board Posts)'.Published &&
        (IsBlank(mtl_publishfrom) || mtl_publishfrom <= Now()) &&
        (IsBlank(mtl_expireson) || mtl_expireson > Now())
    ),
    "mtl_ispinned",
    SortOrder.Descending,
    "createdon",
    SortOrder.Descending
)
```

- Restored `scrBoard.Fill` to `Color.White` after an earlier formula was accidentally placed in that property.

## Current UI state

Power Apps Studio is open on `scrBoard` with `galPosts` selected. The next incomplete task is mapping the gallery fields:

- Title → `Title`
- Subtitle → `Category`
- Body → `Message`

There are no White Board post records yet, so the gallery has no real content to display.

## Next steps for tomorrow

1. Save the Canvas App and complete the gallery field mapping above.
2. Add a header label: `Estimation White Board`.
3. Add a **New post** button.
4. Add an edit screen using a form connected to `White Board Posts`.
5. Configure draft, publish, and archive actions; add post-details navigation.
6. Test with a harmless Draft record, then a Published record.
7. Configure security roles for Estimation team users before sharing the app.
8. Add optional speech-to-text only after the Azure AI Speech/Power Automate configuration is ready.

## Continuation — 25 September 2026

- Confirmed the required read-only Estimation Dev canary: `mtl_scheduledemail` exists at `https://org0e46434e.crm7.dynamics.com/`.
- Exported the current unmanaged `WhiteBoard00Solution` from Estimation Dev as `WhiteBoard00Solution-estimation-dev-current.zip`.
- Unpacked that export into `WhiteBoard00Solution-current-exported/` and verified that it contains both solution root components:
  - `mtl_whiteboardpost` (Dataverse table)
  - `mtl_whiteboard00_b585b` (the `White Board 00` Canvas App)
- The Canvas App artifact is now captured in source as `CanvasApps/mtl_whiteboard00_b585b_DocumentUri.msapp` together with its metadata files. The application is saved in Studio, but changes have not been published or shared.
- Direct GUI control remains unavailable until the Windows policy allows the ChatGPT/Codex desktop application to launch. No Dataverse data, security roles, sharing settings, production deployments, or external messages were changed.

## Dataverse validation — 25 September 2026

- Re-ran the required Estimation Dev canary successfully: `mtl_scheduledemail` exists.
- Confirmed that the White Board table entity set is `mtl_whiteboardposts`.
- Created one harmless, labelled validation record as a **Draft** only:
  - Title: `TEST - White Board draft validation (2026-09-25)`
  - Record ID: `ec5e492d-a3b8-f111-aaac-002248ef8715`
  - Category/Priority: `General` / `Normal`; not pinned; no publish or expiry date.
- Read-back verification confirmed all required values persisted. A query restricted to `Published` status returned zero rows, confirming the Draft is excluded from the default published-board data set.
- The test record was deliberately retained as a Draft. Do not publish, archive, or delete it without explicit approval.
- Repacked the current exported solution successfully as `WhiteBoard00Solution-current-verified-unmanaged.zip`; the package includes both `mtl_whiteboardpost` and Canvas App `mtl_whiteboard00_b585b`.

## Important technical notes

- The workspace Dataverse MCP is not confirmed to target Estimation Dev. Do not use it for writes.
- Direct Dataverse CLI/API calls must explicitly use the Estimation Dev URL and this header:

```text
MSCRM.SolutionUniqueName:WhiteBoard00Solution
```

- The original local `WhiteBoard00Solution/src/Other` folder is a OneDrive read-only reparse point. `pac solution sync` exported successfully but could not unpack into that folder. Do not retry sync there until the OneDrive folder restriction is resolved.
- The current remote source snapshot is safely available in `WhiteBoard00Solution-exported/`.
- Changes made after commit `876281a` are not yet committed or pushed.
